{
  "name": "berber-rezervasyon",
  "version": "1.0.0",
  "description": "Berber Dükkanı Rezervasyon Sistemi",
  "main": "server.js",
  "scripts": {
  "start": "kill-port 4000 && node server.js",
  "dev": "kill-port 4000 && node server.js"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
